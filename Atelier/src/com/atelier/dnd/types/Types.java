package com.atelier.dnd.types;

import com.atelier.dnd.types.alpha.TypeRegistry;

public class Types {
	public static void registerTypes(TypeRegistry registry) {
		registerAbilitySpreads(registry);
		
		registerClasses(registry);
		registerRaces(registry);
		registerCharacterAttributes(registry);
		
		registerItems(registry);
		registerItemAttributes(registry);
	}
	
	private static void registerAbilitySpreads(TypeRegistry registry) {
//		registry.registerType("point-buy-spread", PointBuySpread.class);
	}
	
	private static void registerClasses(TypeRegistry registry) {
		
	}
	
	private static void registerRaces(TypeRegistry registry) {
		
	}
	
	private static void registerCharacterAttributes(TypeRegistry registry) {
		
	}
	
	private static void registerItems(TypeRegistry registry) {
		
	}
	
	private static void registerItemAttributes(TypeRegistry registry) {
		
	}
}
